import { Product, Category, Banner, Store } from '@/types/database';

export const MOCK_BANNERS: Banner[] = [];
export const MOCK_CATEGORIES: Category[] = [];
export const MOCK_STORES: Store[] = [];
export const MOCK_PRODUCTS: Product[] = [];
